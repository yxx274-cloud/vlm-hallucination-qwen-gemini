# Rerun Experiment Record 2026-05

This file records all experimental settings for the 2026-05 rerun batch.

## 1. Unified Settings

| Field | Value |
|---|---|
| prompt_version | v4-2026-05 |
| detector_version | v3.1 (slope_pct key fix) |
| temperature | 0.0 |
| seed | 20230630 |
| anonymize | True (ts_code not in user prompt) |
| end_date | 20230630 |
| stock pool | HS300 N=323 |
| WINDOW_SIZE | 30 trading days |

Old batch (82.3%): temperature=0.1, no seed, prompt had detector keywords, ts_code in prompt.
This batch: temperature=0.0, seed=20230630, neutral prompt, ts_code anonymized.
Expected UCR difference from old batch: +-5pp is normal; >10pp requires investigation.

## 2. Local Machine (Batch A + B)

### Prerequisites (PowerShell)

Already in demo/config.local.py. To override:

  $env:ZHIPU_API_KEY  = "dcc0c99fd0794343ab736a070067ad15.N0QHd6q674mMoNs8"
  $env:NVIDIA_API_KEY = "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"

### Batch A - GLM (bigmodel)

  cd demo

  # GLM-4V-Flash: chart-only x 4 methods
  python run_multi_model_chart_only.py --model-specs "bigmodel:glm-4v-flash" --end-date 20230630 --output-root ..\results_multi_model_chart_only

  # GLM-4V-Flash: trimodal baseline
  python run_vlm_multimodel_batch.py --model-key glm-4v-flash --provider bigmodel --modes text_only,chart_only,multimodal --end-date 20230630

  # GLM-4-Flash (text-only LLM, text_only only)
  python run_vlm_multimodel_batch.py --model-key glm-4-flash --provider bigmodel --modes text_only --end-date 20230630

Smoke test (N=20): append --limit 20 to each command.

### Batch B - Gemma x3 + DeepSeek-V4 (nvidia hosted)

  cd demo

  # Gemma-4-31b-it
  python run_vlm_multimodel_batch.py --model-key gemma-4-31b-it --provider nvidia --modes text_only,chart_only,multimodal --end-date 20230630
  python run_multi_model_chart_only.py --model-specs "nvidia:gemma-4-31b-it" --end-date 20230630 --output-root ..\results_multi_model_chart_only

  # Gemma-3n-e4b-it
  python run_vlm_multimodel_batch.py --model-key gemma-3n-e4b-it --provider nvidia --modes text_only,chart_only,multimodal --end-date 20230630
  python run_multi_model_chart_only.py --model-specs "nvidia:gemma-3n-e4b-it" --end-date 20230630 --output-root ..\results_multi_model_chart_only

  # Gemma-3n-e2b-it
  python run_vlm_multimodel_batch.py --model-key gemma-3n-e2b-it --provider nvidia --modes text_only,chart_only,multimodal --end-date 20230630
  python run_multi_model_chart_only.py --model-specs "nvidia:gemma-3n-e2b-it" --end-date 20230630 --output-root ..\results_multi_model_chart_only

  # DeepSeek-V4-Flash (text-only LLM)
  python run_vlm_multimodel_batch.py --model-key deepseek-v4-flash --provider nvidia --modes text_only --end-date 20230630

  # DeepSeek-V4-Pro (text-only LLM)
  python run_vlm_multimodel_batch.py --model-key deepseek-v4-pro --provider nvidia --modes text_only --end-date 20230630

If deepseek-v4-flash/pro not yet on NVIDIA Build, substitute deepseek-ai/deepseek-r1 and note in ledger.

## 3. Deliver to Advisor (Batch C - Qwen3-VL x3)

### 3.1 Local deployment

  # vLLM (recommended)
  pip install vllm
  vllm serve Qwen/Qwen3-VL-8B-Instruct --port 8000 --max-model-len 8192

  # LMDeploy alternative
  pip install lmdeploy
  lmdeploy serve api_server Qwen/Qwen3-VL-8B-Instruct --server-port 8000

For 2B/4B replace model name accordingly.

### 3.2 Run commands (from repo root)

  # Qwen3-VL-8B
  python demo/run_vlm_multimodel_batch.py --model-key qwen3-vl-8b --provider qwen_local --api-base http://localhost:8000/v1 --modes text_only,chart_only,multimodal --end-date 20230630
  python demo/run_multi_model_chart_only.py --model-specs "qwen_local:qwen3-vl-8b" --api-base http://localhost:8000/v1 --end-date 20230630 --output-root results_multi_model_chart_only

  # Qwen3-VL-4B (restart service with new model)
  python demo/run_vlm_multimodel_batch.py --model-key qwen3-vl-4b --provider qwen_local --api-base http://localhost:8000/v1 --modes text_only,chart_only,multimodal --end-date 20230630
  python demo/run_multi_model_chart_only.py --model-specs "qwen_local:qwen3-vl-4b" --api-base http://localhost:8000/v1 --end-date 20230630 --output-root results_multi_model_chart_only

  # Qwen3-VL-2B
  python demo/run_vlm_multimodel_batch.py --model-key qwen3-vl-2b --provider qwen_local --api-base http://localhost:8000/v1 --modes text_only,chart_only,multimodal --end-date 20230630
  python demo/run_multi_model_chart_only.py --model-specs "qwen_local:qwen3-vl-2b" --api-base http://localhost:8000/v1 --end-date 20230630 --output-root results_multi_model_chart_only

Smoke test (N=20): append --limit 20 to each command.

### 3.3 Copy results back

  demo/outputs/multimodel/qwen_local/
  results_multi_model_chart_only/qwen_local/

## 4. Reconciliation Rule

After Batch A GLM-4V-Flash:
  python analyze_multi_model_week1.py --results-root results_multi_model_chart_only

Check GLM-4V-Flash Forced-Decision chart-only Answered-UCR in summary_table.md:
  Expected: 77%-87% (old batch 82.3% +- 5pp)
  If outside range: check prompt_version / detector_version in JSON records

## 5. Critical Fixes (for reviewer reference)

1. detector slope key mismatch (v3.0->v3.1): trend.get("slope") -> trend.get("slope_pct")
2. batch runner fabricated detection: removed fallback ucr=1.0 when OHLCV unavailable
3. baseline prompt self-fulfilling: removed detector keyword bullets from prompt
4. blind control exposed real stock codes: ts_label replaced with STOCK_X
5. JSON missing reproducibility metadata: added prompt_version, detector_version, sampling fields